spring:
  application:
    name: gj-auth
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev, mysql}
logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client: warn